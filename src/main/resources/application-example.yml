spring:
  application:
    name: board0811

  datasource:
    url: jdbc:mysql://localhost:3306/springboard?useSSL=false&serverTimezone=Asia/Seoul
    username: USERNAME
    password: PASSWORD
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

  web:
    resources:
      add-mappings: false
